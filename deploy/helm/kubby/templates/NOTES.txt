Kubby {{ .Values.image.tag | default .Chart.AppVersion }} is installed.

{{- if .Values.ingress.enabled }}

  {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}
{{- else }}

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kubby.fullname" . }} 8080:{{ .Values.service.port }}
  {{ .Values.config.publicUrl }}
{{- end }}

Check it came up:

  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/instance={{ .Release.Name }} | grep -i schema

The first browser visit creates the first administrator.

{{- if not .Values.secrets.existingSecret }}

WARNING  The encryption key was generated by the chart and lives in Helm's release
         history. `helm upgrade` will regenerate it unless it is passed in again, and a
         new key cannot open what the old one sealed. Create a Secret yourself and set
         secrets.existingSecret before you register a cluster.
{{- end }}
{{- if .Values.config.readOnly }}

NOTE     KUBBY_READ_ONLY is on. Nobody can write to any cluster, administrators included.
{{- end }}
